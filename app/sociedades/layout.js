import './globals.css';

export const metadata = {
  title: 'Gestión de Sociedades — DCAC',
  description: 'Tablero de tráfico, operaciones ganaderas y funnel de prospectos',
};

export default function SociedadesLayout({ children }) {
  return (
    <div>
      {children}
    </div>
  );
}
